В решении данного домашнего задания использовались библиотеки:
```

REQUESTS (module_11_1):
Итог:
1) Класс (requests.Session) — удобно для работы с постоянными параметрами (куки, заголовки, базовый URL).
2) Метод (requests.get) — стандартные вызовы для отправки запросов (GET, POST и т.д.).
3) Функция (requests.request) — универсальный способ отправки запросов любого типа.


PANDAS (module_11_1_2):
Итог: 
1) В примере 1 используется класс DataFrame для создания таблицы данных.
2) В примере 2 вызывается метод mean(), который вычисляет среднее значение для столбца.
3) В примере 3 применяется функция read_csv() для чтения данных из CSV-файла.

NUMPY (module_11_1_3):
Итог:
1) В примере 1 использовался класс numpy.ndarray для создание массива
2) В примере 2 использовался метод reshape для изменения формы массива
3) В примере 3 использовалась функция numpy.dot для умножения матриц

```